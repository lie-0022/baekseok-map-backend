spring:
  # === (??) PostgreSQL ?? (?? ?? ??) ===
  # datasource:
  #   url: jdbc:postgresql://localhost:5432/[DB_??]
  #   username: [?_PostgreSQL_ID]
  #   password: [?_PostgreSQL_????]
  #   driver-class-name: org.postgresql.Driver

  # === (??) H2 ???? DB ?? ===
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb  # 'testdb'?? ??? ???? DB ??
    username: sa
    password:

  # === H2 ? ?? ??? ===
  h2:
    console:
      enabled: true
      path: /h2-console # http://localhost:8080/h2-console ? ??

  # === JPA (?????? ???) ?? (??? ??) ===
  jpa:
    hibernate:
      ddl-auto: create # create? ??!
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database-platform: org.hibernate.dialect.H2Dialect # PostgreSQL?? H2? ??

# === JWT ?? ? ?? (??? ??) ===
jwt:
  secret: 'YmFlazNlMGstbWFwLXByb2plY3QtaXMtdmVyeS1mdW4tYW5kLXNlY3JldC1rZXktMjAyNSE='